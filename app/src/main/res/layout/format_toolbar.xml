<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    android:id="@+id/format_toolbar"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="@dimen/format_toolbar_height"
    android:background="@color/format_toolbar_background"
    android:elevation="@dimen/format_toolbar_elevation"
    tools:targetApi="lollipop">

    <LinearLayout
        style="@style/MyFormatToolbar"
        android:layout_gravity="center_horizontal"
        android:orientation="horizontal"
        tools:ignore="UselessParent">

        <ImageButton
            android:id="@+id/format_bold"
            style="?attr/borderlessButtonStyle"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackground"
            android:contentDescription="@string/format_bold"
            app:srcCompat="@drawable/format_bold"
            android:tint="#6E6E6E"
            />

        <ImageButton
            android:id="@+id/format_italic"
            style="?attr/borderlessButtonStyle"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackground"
            android:contentDescription="@string/format_italic"
            app:srcCompat="@drawable/format_italic"
            android:tint="#6E6E6E"
            />

        <ImageButton
            android:id="@+id/format_link"
            style="?attr/borderlessButtonStyle"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackground"
            android:contentDescription="@string/format_link"
            app:srcCompat="@drawable/format_link"
            android:tint="#6E6E6E"
            />

        <ImageButton
            android:id="@+id/format_image"
            style="?attr/borderlessButtonStyle"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackground"
            android:contentDescription="@string/format_image"
            app:srcCompat="@drawable/format_image"
            android:tint="#6E6E6E"
            />

    </LinearLayout>

</FrameLayout>
